name: Lint, Test & Deploy
on: push
jobs:
  lint-test-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check Out
        uses: actions/checkout@v3
      - name: Lint
        run: npm lint
      - name: Test
        run: npm test
      - name: Deploy
        run: npm build